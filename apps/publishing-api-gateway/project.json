{
  "name": "publishing-api-gateway",
  "root": "apps/publishing-api-gateway",
  "sourceRoot": "apps/publishing-api-gateway",
  "projectType": "application",
  "targets": {
    "serve": {
      "executor": "nx:run-commands",
      "options": {
        "command": "nest start publishing-api-gateway --watch"
      }
    },
    "start": {
      "executor": "nx:run-commands",
      "options": {
        "command": "nest start publishing-api-gateway"
      }
    },
    "build": {
      "executor": "nx:run-commands",
      "options": {
        "command": "nest build publishing-api-gateway"
      }
    }
  }
}
